"""Stub for `sqlalchemy.orm` used in tests."""

class Session:
    """A minimal placeholder Session used only for type compatibility in tests."""
    def __init__(self, *args, **kwargs):
        pass
